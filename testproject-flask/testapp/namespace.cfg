[namespace_setting]
type = string
required = False
max_length = 8
label = Namespace setting
help_text = Enter any word, but not more 8 symbols length (not required).

[setting_to_redefine]
type = int
default = 0
min_value = 0
max_value = 32
label = Setting to redefine
help_text = This setting would be redefined in project configuration definition file.
